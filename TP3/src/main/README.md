# Codigo de produccion

`java/` contiene exclusivamente el codigo necesario para ejecutar el trabajo.
Si se incorporan recursos de runtime que no son Java, crear `resources/` junto
a `java/` y documentar su formato y procedencia.

Evitar constantes experimentales escondidas en el codigo: los parametros de
la simulacion deben ser configurables y quedar registrados en cada salida.

